{
  "name": "mock-external-api",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone mock of the Customers/Products/Shipments API described in the OpenAPI spec. Stands in for the external team's service during local dev and tests.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node-dev": "^2.0.0"
  }
}
